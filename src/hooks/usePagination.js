import { useState } from "react";

const usePagination = () => {
  // const [page, setPage] =

  const handleChange = (token) => {
    // setPage(token);
  };

  return {
    // page,
    handleChange,
  };
};

export default usePagination;
